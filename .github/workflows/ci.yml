name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Lint
        run: pnpm exec biome ci .
      - name: Build all packages (turbo respects topological order)
        run: pnpm exec turbo run build --filter='./packages/*'
      - name: Test all packages
        run: pnpm exec turbo run test --filter='./packages/*'
      - name: Build example
        run: pnpm --filter @example/express-react build
      - name: E2E example smoke
        run: pnpm --filter @example/express-react test
