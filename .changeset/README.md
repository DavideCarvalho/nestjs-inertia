# Changesets

This directory contains "changesets" — small markdown files describing the changes you made and which packages they affect.

Run `pnpm changeset` to create a new changeset for your changes.
